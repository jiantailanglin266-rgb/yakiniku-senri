# 既存画像の棚卸し

Wikimedia Commons の導入にあたり、**先に既存の画像を確認**しました。

> **既存画像は1件も削除していません。**
> 出自が記録されていないことと、権利上の問題があることは別です。
> まず記録を作り、確認できたものから明示していきます。

## 1. 数量

| 形式     | 件数   |
| -------- | ------ |
| webp     | 78     |
| png      | 9      |
| svg      | 3      |
| **合計** | **90** |

ディレクトリ別:

| ディレクトリ                                                                | 件数 | 内容                       |
| --------------------------------------------------------------------------- | ---- | -------------------------- |
| `flags/`                                                                    | 42   | 言語切り替え用の国旗       |
| `common/`                                                                   | 10   | 共通（OGP・ロゴ等）        |
| `gallery/`                                                                  | 8    | 千里 店舗・料理            |
| `cardport/`                                                                 | 6    | CARD PORT 用に生成した画像 |
| `commitment/`                                                               | 4    | 千里 こだわり              |
| `hero/` `menu/` `story/`                                                    | 各3  | 千里                       |
| `ai-port/`                                                                  | 2    | AI PORT                    |
| その他（`access/` `brand/` `movie/` `news/` `owner/` `portal/` `takeout/`） | 各1  | —                          |

## 2. 分かったこと

### 2-1. 出自の記録が存在しない

**90件すべてについて、撮影者・提供元・撮影日・利用条件の記録がありません。**
リポジトリ内に、画像の権利情報を保持する仕組み自体がありませんでした
（`src/media/` はそれを作るためのものです）。

- 千里の店舗・料理写真（約25件）は、店舗自身の素材と推測されますが、
  **推測なので記録には書きません。** 撮影者の確認が必要です。
- 国旗（42件）は各国の国旗デザインで、意匠そのものは一般に著作権の対象外とされますが、
  **SVGファイルとしての著作物性は別です。** 追跡した結果、
  [flag-icons](https://github.com/lipis/flag-icons)（MIT License）の SVG を
  WebP へ変換したものと判明しました。MIT は著作権表示とライセンス文の保持を
  求めるため、表記が無い状態はライセンス違反にあたります。
  → `src/media/data/third-party.ts` に記録し、各サイトの `/image-credits` に
  著作権表示・配布元・ライセンス・加工内容を掲載して解消しました。
- `cardport/` の6件はこのリポジトリの `scripts/generate-cardport-assets.mjs` が
  生成したもので、外部素材を含みません。

### 2-2. Wikimedia の画像は 1 件も使っていない

既存の90件に、Wikimedia Commons 由来のファイルはありません。
したがって、**今回の対応で「既存のWikimedia画像を差し替える」作業は発生しません。**

### 2-3. クレジット表示の仕組みが無かった

画像に作者・出典・ライセンスを添えるコンポーネントが存在しませんでした。
`ImageAttribution` / `ImageSourceDetails` / `/image-credits` で新設しています。

### 2-4. YouTube サムネイルの外部参照

`i.ytimg.com` のURLが構造化データとフィードに含まれています
（`src/cardport/lib/structured-data.ts` / `feeds.ts` / `src/portal/lib/structured-data.ts` /
`src/lib/ai-port/youtube.ts`）。

- 構造化データ・RSS の `<image>` は、YouTube が定める埋め込み・参照の範囲です。
- ただし**ページ本文でのホットリンクは行わない**方針にしました。
  CARD PORT の動画一覧は `MediaSlot`（装飾）に置き換え済みです。

## 3. 対応方針

| 対象                  | 対応                                                          | 状態                    |
| --------------------- | ------------------------------------------------------------- | ----------------------- |
| 千里の店舗・料理写真  | 撮影者・提供元を店舗に確認し、記録を作る                      | 未着手（要確認）        |
| 国旗                  | flag-icons（MIT）と特定。著作権表示を `/image-credits` に掲載 | 完了                    |
| CARD PORT 生成画像    | 生成スクリプトが出所。記録済み（外部素材なし）                | 完了                    |
| YouTube サムネイル    | 本文でのホットリンクをやめる                                  | CARD PORT は完了        |
| 新規の Wikimedia 画像 | `src/media/` の判定を通ったものだけ掲載                       | 仕組みは完了・画像は0件 |

## 4. 未確認のものを「確認済み」に見せない

`/card-port/<言語>/image-credits` は、
**ライセンスを確認できた画像だけ**を並べます。

現在は0件なので、一覧の代わりに
「まだ確認できた画像はない」旨をそのまま表示しています。
それらしい一覧を作って「確認済み」に見せることはしません。
